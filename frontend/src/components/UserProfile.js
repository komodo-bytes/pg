import React from 'react';

const UserProfile = () => {
    return (
        <div>
            <h1>User Profile</h1>
            {/* Add user profile detail here */}
        </div>
    );
};

export default UserProfile;