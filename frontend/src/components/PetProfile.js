import React from 'react';

const PetProfile = () => {
    return (
        <div>
            <h1>Pet Profile</h1>
            {/* Add pet profile detail here */}
        </div>
    );
};

export default PetProfile;